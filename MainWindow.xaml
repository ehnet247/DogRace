<Window x:Class="DogRace.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DogRace"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="1400">
    <Window.Resources>
        <Style x:Key="SliderRepeatButton" TargetType="RepeatButton">
            <Setter Property="SnapsToDevicePixels" Value="true" />
            <Setter Property="OverridesDefaultStyle" Value="true" />
            <Setter Property="IsTabStop" Value="false" />
            <Setter Property="Focusable" Value="false" />
        </Style>

        <Style x:Key="SliderRepeatButton1" TargetType="RepeatButton">
            <Setter Property="SnapsToDevicePixels" Value="true" />
            <Setter Property="OverridesDefaultStyle" Value="true" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border SnapsToDevicePixels="True" Background="Green"  BorderThickness="1" BorderBrush="YellowGreen" Height="3"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SliderThumb" TargetType="Thumb">
            <Setter Property="SnapsToDevicePixels" Value="true" />
            <Setter Property="OverridesDefaultStyle" Value="true" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Ellipse Height="30" Width="30">
                            <Ellipse.Fill>
                                <ImageBrush ImageSource="/dog-icon-35935.png"></ImageBrush>
                            </Ellipse.Fill>
                        </Ellipse>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <ControlTemplate x:Key="Slider"  TargetType="Slider">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" MinHeight="{TemplateBinding MinHeight}" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Track Grid.Row="1" x:Name="PART_Track"   >
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Style="{StaticResource SliderRepeatButton1}"  Command="Slider.DecreaseLarge" />
                    </Track.DecreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb Style="{StaticResource SliderThumb}"  />
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Style="{StaticResource SliderRepeatButton}" Command="Slider.IncreaseLarge" />
                    </Track.IncreaseRepeatButton>
                </Track>
            </Grid>
        </ControlTemplate>

        <Style x:Key="Horizontal_Slider" TargetType="Slider">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="SnapsToDevicePixels" Value="true" />
            <Setter Property="OverridesDefaultStyle" Value="true" />
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="MinHeight" Value="21" />
                    <Setter Property="MinWidth" Value="104" />
                    <Setter Property="Template" Value="{StaticResource Slider}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Window.DataContext>
        <local:DogRaceViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="50"/>
        </Grid.ColumnDefinitions>

        <!-- Dogs names-->
        <TextBox Text="{Binding Dog1Name}" FontSize="18" Height="30" Grid.Row="0" Grid.Column="0"/>
        <TextBox Text="{Binding Dog2Name}" FontSize="18" Height="30" Grid.Row="1" Grid.Column="0"/>
        <TextBox Text="{Binding Dog3Name}" FontSize="18" Height="30" Grid.Row="2" Grid.Column="0"/>
        <TextBox Text="{Binding Dog4Name}" FontSize="18" Height="30" Grid.Row="3" Grid.Column="0"/>
        <TextBox Text="{Binding Dog5Name}" FontSize="18" Height="30" Grid.Row="4" Grid.Column="0"/>
        <!-- Dogs simulation -->
        <Slider x:Name="dog1Slider" Value="{Binding Dog1Position, Mode=OneWay}" Style="{StaticResource Horizontal_Slider}" Minimum="0" Maximum="1000" Height="30" Margin="10" Grid.Row="0" Grid.Column="1"/>
        <Slider x:Name="dog2Slider" Value="{Binding Dog2Position, Mode=OneWay}" Style="{StaticResource Horizontal_Slider}"  Minimum="0" Maximum="1000" Height="30" Margin="10" Grid.Row="1" Grid.Column="1"/>
        <Slider x:Name="dog3Slider" Value="{Binding Dog3Position, Mode=OneWay}" Style="{StaticResource Horizontal_Slider}" Minimum="0" Maximum="1000" Height="30" Margin="10" Grid.Row="2" Grid.Column="1"/>
        <Slider x:Name="dog4Slider" Value="{Binding Dog4Position, Mode=OneWay}" Style="{StaticResource Horizontal_Slider}" Minimum="0" Maximum="1000" Height="30" Margin="10" Grid.Row="3" Grid.Column="1"/>
        <Slider x:Name="dog5Slider" Value="{Binding Dog5Position, Mode=OneWay}" Style="{StaticResource Horizontal_Slider}" Minimum="0" Maximum="1000" Height="30" Margin="10" Grid.Row="4" Grid.Column="1"/>
        <!-- Dogs scores-->
        <TextBlock Text="{Binding Dog1Score}" FontSize="20" Visibility="{Binding ScoresVisibility}" Grid.Row="0" Grid.Column="2"/>
        <TextBlock Text="{Binding Dog2Score}" FontSize="20" Visibility="{Binding ScoresVisibility}" Grid.Row="1" Grid.Column="2"/>
        <TextBlock Text="{Binding Dog3Score}" FontSize="20" Visibility="{Binding ScoresVisibility}" Grid.Row="2" Grid.Column="2"/>
        <TextBlock Text="{Binding Dog4Score}" FontSize="20" Visibility="{Binding ScoresVisibility}" Grid.Row="3" Grid.Column="2"/>
        <TextBlock Text="{Binding Dog5Score}" FontSize="20" Visibility="{Binding ScoresVisibility}" Grid.Row="4" Grid.Column="2"/>
        <!-- Buttons -->
        <StackPanel Orientation="Horizontal" Grid.Row="5" Grid.ColumnSpan="3">
            <Button Content="Start" Command="{Binding StartCommand}" Width="100" Height="30" Background="White" Margin="5"/>
            <Button Content="Stop" Command="{Binding StopCommand}" Width="100" Height="30" Background="White" Margin="5"/>
            <Button Content="Reset" Command="{Binding ResetCommand}" Width="100" Height="30" Background="White" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
